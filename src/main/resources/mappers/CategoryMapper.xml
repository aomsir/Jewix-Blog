<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aomsir.jewixapi.mapper.CategoryMapper">

    <select id="queryCategoryParentCount" resultType="Integer">
        SELECT COUNT(id)
        FROM tb_category
        WHERE parent_id = 0
    </select>

    <select id="queryCategoryParentListByPage" resultType="Category">
        SELECT id,category_name,parent_id,create_time,update_time
        FROM tb_category
        WHERE parent_id = 0
        LIMIT #{param.start},#{param.length}
    </select>
</mapper>