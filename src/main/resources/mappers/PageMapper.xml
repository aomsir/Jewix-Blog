<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aomsir.jewixapi.mapper.PageMapper">



    <select id="queryPageList" resultType="Page">
        SELECT id, name, model, author, omit, content, create_time, update_time
        FROM tb_page
        WHERE 1=1
    </select>

    <select id="queryPageByOmit" resultType="Page">
        SELECT id, name, model, author, omit, content, create_time, update_time
        FROM tb_page
        WHERE omit = #{omit}
    </select>
    <select id="queryPageById" resultType="Page">
        SELECT id, name, model, author, omit, content, create_time, update_time
        FROM tb_page
        WHERE id = #{id}
    </select>

    <insert id="insertPage">
        INSERT INTO tb_page (name, model, author, omit, content, create_time, update_time)
        VALUES (#{name}, #{model}, #{author}, #{omit}, #{content}, #{createTime}, #{updateTime})
    </insert>

    <update id="updatePage">
        UPDATE tb_page
        <if test="name != null">
            name = #{param.name},
        </if>
        <if test="model != null">
            model = #{param.model},
        </if>
        <if test="author != null">
            author = #{param.author},
        </if>
        <if test="omit != null">
            omit = #{param.omit},
        </if>
        <if test="content != null">
            content = #{param.content},
        </if>
        <if test="updateTime != null">
            update_time = #{param.updateTime},
        </if>
        WHERE id = #{id}
    </update>


    <delete id="deletePage">
        DELETE FROM tb_page
        WHERE id = #{id}
    </delete>

    <delete id="deletePageByIdAndOmit">
        DELETE FROM tb_page
        WHERE id = #{id}
        AND omit = #{omit}
    </delete>
</mapper>